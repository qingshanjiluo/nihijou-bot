version: '3.8'

services:
  bot:
    build: .
    container_name: nichijou-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    environment:
      - HEADLESS=true
      - SLOW_MO=50